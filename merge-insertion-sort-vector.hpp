#ifndef MERGE_INSERTION_SORT_VECTOR_HPP
#define MERGE_INSERTION_SORT_VECTOR_HPP
#include <iostream>
#include <vector>
std::vector<int>	mergeInsertionSort(std::vector<int> &vct, size_t step = 1);
#endif
